package udemy

// Scope - creates and runs coroutines , provides lifecycle events (starting, cancelling, stopping)
// Context - the scope provides a context in which coroutines run (Scope and coroutine are tightly bound together)
// suspending functions - functions that can be run in a coroutine
// Job - is a handle on a coroutine. (will be created when coroutine is created)
// Deferred - a future result of a coroutine
// Dispatcher - manages which threads the coroutine runs on
// Error handling  - try/catch
